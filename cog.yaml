# Configuration for Cog ⚙️
build:
  gpu: true
  cuda: "12.2"
  python_version: "3.11"
  system_packages:
    - "libgl1-mesa-glx"
  python_packages:
    - "torch"
    - "transformers"
    - "diffusers"
    - "accelerate"
    - "safetensors"
    - "opencv-python"

predict: "predict.py:Predictor"
